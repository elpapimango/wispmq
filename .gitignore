# Rust build artifacts
/target

# SQLite databases (runtime state) and WAL sidecars
*.db
*.db-wal
*.db-shm

# Windows "mark of the web" alternate-data-stream artifacts
*:Zone.Identifier

# Local config (may hold secrets); the tracked template is *.example.yaml
pulsemq.yaml
pulsemq.yml
